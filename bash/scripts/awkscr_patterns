# output lines that meet this pattern
/MA/ 

# then apply this logic - this was processed on the whole 
{
    # print fields
    print "\t- field_1: " $1;
    print "\t- field_2: " $2;
    print "\t- field_3: " $3;
    
}